================================================================================
 STUDY REPORT
 Self-Supervised Representation Learning under Varying Data Distributions
 for Reduction of Artefact-Driven False Alarms in Rodent EEG Seizure Detection
================================================================================

Document status : LIVING DOCUMENT. This file is updated as each method,
                  parameter, and result is finalised during implementation.
                  Values marked [TBD] are fixed on the validation partition
                  before any test-set evaluation and recorded here when set.
Companion work  : Baseline project DL_WITH_SSL_GA (Multi-Scale TCN / M3).
                  This study reuses the M3 encoder, data pipeline, and
                  evaluation utilities of that project without modification,
                  so that the baseline (R0) is a valid reference point.

--------------------------------------------------------------------------------
1. MOTIVATION AND PROBLEM STATEMENT
--------------------------------------------------------------------------------

Continuous single-channel rodent EEG monitoring produces recordings in which
annotated seizure activity occupies well under 1% of total time (raw
ictal:non-ictal ratio approximately 1:438 in the baseline corpus). To train
the baseline Multi-Scale TCN (M3) under this extreme imbalance, the baseline
project applied PROXIMITY-AWARE DOWNSAMPLING (baseline STUDY_REPORT Section 9;
create_balanced_splits.py): all ictal segments and all non-ictal segments
within +/-60 s of a seizure ("near-seizure") are retained, while non-ictal
segments further than 60 s from any seizure ("far-from-seizure") are randomly
subsampled to only 5% of the near-seizure count.

Consequence. The resulting training corpus (220,653 segments; ictal:non-ictal
1:2.37) is dominated by peri-ictal background (95.3% of the non-ictal class).
Behavioural artefacts -- grooming, chewing, movement, electrode/muscle
activity -- occur predominantly during normal behaviour spread across the
whole recording, i.e. mostly FAR from seizures. Because the far pool is
reduced to roughly 3.3% of the corpus, the vast majority of isolated artefact
windows are discarded. The classifier is therefore trained almost exclusively
to discriminate seizure from near-seizure background and seldom learns the
appearance of artefacts. At deployment on the full, un-downsampled stream,
transient high-morphology artefacts are effectively out-of-distribution and
are frequently mis-fired as seizures. This artefact-driven FALSE ALARM is the
failure mode targeted by the present study.

Central idea. Exposing the ENCODER to the full data distribution -- including
artefacts in their natural abundance -- through self-supervised representation
learning (SSL) should yield a representation in which artefacts are separable
from seizures, thereby reducing false alarms without requiring artefact
labels (which do not exist: artefacts are unlabeled members of the non-ictal
class).

--------------------------------------------------------------------------------
2. OBJECTIVES AND HYPOTHESES
--------------------------------------------------------------------------------

Primary objective. Determine whether, and by what mechanism, self-supervised
pretraining on broader data distributions reduces artefact-driven false alarms
in rodent EEG seizure detection, relative to the supervised M3 baseline.

Hypotheses.
  H1  A self-supervised objective improves the representation even when the
      pretraining data is held identical to the baseline (R0 -> R1).
  H2  Exposing the encoder to the full distribution (with artefacts) improves
      artefact separation beyond the objective alone (R1 -> R2).
  H3  Allowing the classification head and decision threshold to also see a
      broadened background unlocks false-alarm reduction that a narrow
      fine-tune cannot (R2 -> R3).
  H4  Any gain is not merely an effect of broader fine-tuning data without SSL
      (R4 vs R0).
  H5  (Headline) With both models exposed to broad fine-tuning data, SSL
      pretraining reduces false alarms relative to no SSL (R3 vs R4).
  H6  SSL improves the learned REPRESENTATION, not only the optimisation /
      initialisation (frozen linear-probe vs full fine-tune, all runs).

--------------------------------------------------------------------------------
3. DATA DISTRIBUTIONS
--------------------------------------------------------------------------------

All distributions derive from the same preprocessed segment store of the
baseline project: single-channel EEG, 500 Hz, 5-second windows (2,500 samples)
with 50% overlap, 50 Hz notch, DC removal, robust z-score normalisation. Labels
are binary (ictal / non-ictal); artefacts are unlabeled non-ictal segments.

  D0     Proximity-aware downsampled corpus (baseline training manifest,
         data_splits_nonictal_sampled.json). 220,653 segments; near-seizure-
         heavy; far-from-seizure background ~3.3%. This is the exact set on
         which R0 was trained.

  D_full Full unlabeled TRAIN-partition corpus (~28.7M segments at ~0.23%
         natural ictal prevalence; the untouched seizure/ and non_seizure/
         directories in natural abundance). Contains artefacts at their true
         prevalence. Used ONLY for self-supervised pretraining (labels are not
         consumed by SSL; gradients from train subjects only).

  D_broad Broadened-background fine-tuning corpus. Construction (set-difference;
         build_broad_manifest.py): D_broad = D0 (all ictal + all D0 non-ictal)
         PLUS extra FAR-from-seizure background drawn from (full-corpus non-ictal
         MINUS D0), within the SAME 71 training mice (m254 excluded, as D0). The
         broadening level is a target non-ictal:ictal ratio R (D0 is 1:2.37);
         R=6 (CONFIRMED 2026-07-25) -> ~393k non-ictal. Labeled; val/test copied
         unchanged from the filtered+enriched manifest. The supervised recipe
         (incl. pos_weight=1.0) is held constant, so R is a disclosed design
         choice, NOT tuned on validation.

The held-out validation and test partitions are the FULL, un-downsampled
stream and are never downsampled, so evaluation reflects deployment conditions.

3.1 Data split and manifest mapping (REUSED from baseline; identical to R0)

The train/val/test split is PURELY FOLDER-MEMBERSHIP and subject-wise by
construction (generate_data_splits.py): the five TRAIN_DATA* folders form the
TRAIN partition, VAL_DATA the VAL partition, TEST_DATA the TEST partition. There
is no RNG in the split; subject identity is parsed as mouse_id = filename before
the first "_", and a set-intersection LEAKAGE CHECK guarantees no mouse appears
in more than one partition. Because assignment is deterministic folder-
membership, this project REUSES the baseline manifests unchanged -> the split is
byte-identical to R0's, which is required for a valid baseline comparison. No
new split is generated.

CONFIRMED PRESENT on the cluster (data_splits_outputs/): data_splits.json,
data_splits_full_train_enriched.json, data_splits_nonictal_sampled.json,
data_splits_nonictal_sampled_filtered.json,
data_splits_nonictal_sampled_filtered_enriched.json, plus create_balanced_splits
.log and splits_generation.log. Every manifest this project needs already
exists; ONLY D_broad remains to be built.

Manifest mapping (cluster dir /scratch/22206468/INPUT_DATA/data_splits_outputs/):

  Distribution / partition        Baseline manifest              Partition key
  ------------------------------  -----------------------------  -------------
  D0 (proximity-downsampled)      data_splits_nonictal_sampled   train
                                  .json  (or ..._filtered_
                                  enriched.json for chronology)
  D_full (full unlabelled, SSL)   data_splits.json (raw) OR      train
                                  data_splits_full_train_
                                  enriched.json (with time meta)
  D_broad (broadened background)  NEW manifest built from        train
                                  data_splits.json train
                                  [TBD-4]
  Val / Test (held-out eval)      data_splits_nonictal_sampled_  val / test
                                  filtered_enriched.json

Notes.
  * The original non_seizure/ folders were deliberately preserved on the cluster
    for SSL (create_balanced_splits.py); D_full reads them at natural abundance.
  * chrono_idx and t_start_sec (per-segment time index needed for event-level
    evaluation and the min-event sweep) are added by build_chronology.py +
    enrich_manifest.py; use the *_enriched.json manifests wherever event-level
    metrics are computed.
  * Val/test are the FULL partitions minus only extreme-amplitude / NaN segments
    (apply_val_test_filter.py, |x|>1000 or non-finite; <0.075% of val removed);
    they are NEVER proximity-downsampled.
  * Validation used to MONITOR SSL reconstruction loss = VAL_DATA (val
    partition), no gradients (Section 5.4).
  * Per-partition counts (RESOLVED [TBD-8]; audit_manifests.py, 2026-07-25):
      TRAIN (full corpus) 28,752,220 segs; 65,510 ictal / 28,686,710 non-ictal;
                          72 mice; 0.228% ictal.
      D0 (downsampled)    220,653 segs; 65,510 ictal / 155,143 non-ictal;
                          71 mice (m254 excluded); 29.69% ictal.
      VAL (filtered)      4,294,953 segs; 11,603 ictal; 10 mice; 0.270% ictal.
      TEST (filtered)     9,753,796 segs; 15,238 ictal; 20 mice; 0.156% ictal.
      Subject-level leakage check PASSED on every manifest.
  * D_full ENUMERATION (RESOLVED): data_splits.json and
    data_splits_full_train_enriched.json ENUMERATE every segment (all
    28,686,710 non-ictal listed individually) -> the SSL loader reads the
    manifest DIRECTLY; no folder scan. The enriched variant also carries
    chronology (chrono_idx, t_start_sec).
  * m254: the full-corpus train has 72 mice, INCLUDING m254 (0 seizures), which
    D0 excluded. For label-free SSL pretraining m254 is RETAINED (more
    unlabelled data, no downside); D0 and all supervised fine-tuning keep 71.

--------------------------------------------------------------------------------
4. ENCODER ARCHITECTURE (REUSED FROM BASELINE, UNCHANGED)
--------------------------------------------------------------------------------

The encoder is the Multi-Scale TCN (M3) of the baseline project
(tcn_utils.py, class MultiScaleTCN), reused without architectural change so
that R0 remains a valid reference and all runs share an identical backbone.

  * Three parallel dilated causal-convolution branches (fine / intermediate /
    coarse), dilations [1,2,4] / [8,16,32] / [32,64,128]; three CausalConvBlocks
    per branch; LayerNorm over channels; GELU; spatial Dropout1d; residual
    connections. Coarse branch receptive field ~5.38 s spans the full window.
  * Fusion (concat + 1x1 conv, or average) -> global average pooling over time
    -> single linear classification head nn.Linear(num_filters, 1).
  * Encoder / head separation is built in via return_embedding=True, which
    returns the pooled (batch, num_filters) feature vector and skips the head.

Two tap points are used:
  (i)  pooled vector  (batch, num_filters)          -- classification, probing,
                                                       embedding analysis.
  (ii) pre-pool feature map (batch, num_filters, T) -- available for span-level
                                                       reconstruction if needed
                                                       by the SSL decoder.

Hyperparameters (num_filters, kernel_size, dropout, fusion) are inherited from
the baseline's selected configuration so that the encoder capacity is identical
across R0-R4.

--------------------------------------------------------------------------------
5. SELF-SUPERVISED REPRESENTATION LEARNING METHOD
--------------------------------------------------------------------------------

5.1 Method: Masked EEG Autoencoding (masked-span reconstruction)

The encoder is pretrained by masking contiguous spans of the 5-second input
window and reconstructing the original signal from the unmasked context. A
lightweight decoder is attached to the encoder for pretraining and discarded
afterwards; only the encoder weights are carried forward.

5.2 Rationale (why masked reconstruction rather than contrastive learning)

Contrastive representation learning (e.g. SimCLR/TS-TCC/TS2Vec) was considered
and rejected as the primary method for three task-specific reasons:

  (a) Mechanistic alignment. To reconstruct masked spans drawn from the full
      distribution, the encoder must internally model artefact morphology.
      This maps directly onto the study's premise -- "expose the encoder to
      the full distribution."
  (b) No pair construction. The corpus is ~99% background; contrastive
      negatives are almost all background, so instance discrimination pushes
      apart semantically identical background windows (the false-negative
      problem), injecting label noise. Masked reconstruction requires no
      positive/negative pairs and is immune to this.
  (c) No augmentation invariance. Contrastive learning teaches invariance to
      its augmentations; an augmentation that can turn seizure-like into
      artefact-like morphology would collapse the very distinction the study
      seeks to sharpen. Masked reconstruction imposes no such invariance.

Masked autoencoding is thus both more suitable and more defensible for the
false-alarm objective, while remaining a mainstream, well-cited SSL paradigm.

5.3 Pretraining configuration (APPROVED [TBD-1,2,3]; 2026-07-25)

  * Architecture: M3 encoder tapped at the PRE-POOL feature map (batch,
    num_filters, 2500) -- branches + fusion reused from the vendored
    MultiScaleTCN (classification still uses the pooled vector). M3
    hyperparameters (num_filters, kernel_size, dropout, fusion) are LOADED from
    best_multiscale_params.json so the encoder is identical to R0. Decoder:
    2x Conv1d (kernel 3, GELU), num_filters -> num_filters -> 1; discarded after
    pretraining.
  * Masking [TBD-1]: contiguous SPAN masking; ratio 50%; span length 50 samples
    (0.1 s; ~25 spans / 2500-sample window); masked spans set to 0 (robust-z
    baseline) in the encoder input.
  * Reconstruction target [TBD-2]: TIME-DOMAIN signal (primary). Loss: MSE on
    MASKED positions only (MAE convention). Spectral/STFT target deferred to the
    A1 ablation.
  * Optimiser / schedule [TBD-3]: AdamW, lr 1e-3, weight_decay 0.05; cosine
    decay with 5,000-step warmup; batch 256.
  * BUDGET: FIXED 100,000 STEPS (not epochs), held IDENTICAL across R1 (D0) and
    R2/R3 (D_full) -- so R1->R2 isolates DATA distribution, not compute
    (~25.6M windows seen at batch 256; ~0.9 pass of D_full, ~116 passes of D0).
    No D_full subsampling; shuffled streaming.
  * Monitoring: validation RECONSTRUCTION loss (best = lowest). Checkpoint /
    resume per the Section 13 infrastructure conventions.
  * Which runs pretrain on which data: R1 on D0; R2 and R3 on D_full (shared
    encoder, trained once).
  * Held constant across R1-R3: masking, decoder, loss, optimiser, schedule,
    step budget. Only ENCODER INITIALISATION and DATA vary between runs.

5.4 Data provenance and no-leakage policy (pretraining)

  * SSL pretraining GRADIENTS come from TRAINING-PARTITION SUBJECTS ONLY.
    Although the objective is label-free, no gradient update ever uses
    validation or test signals, preserving the baseline's subject-level split
    and eliminating encoder-side leakage.
  * The VALIDATION PARTITION is used to monitor and select the SSL checkpoint
    via reconstruction loss (no gradients). Monitoring on validation is
    preferred over a held-out training slice because it measures reconstruction
    generalisation to UNSEEN SUBJECTS. Validation therefore serves as the
    development set throughout the study (SSL monitoring + fine-tune early
    stopping).
  * The TEST PARTITION is untouched during both pretraining and fine-tuning; it
    is the only fully held-out set and supplies all headline numbers.

5.5 Training diagnostics reported (pretraining)

  * Train and validation RECONSTRUCTION LOSS curves per epoch (masked-position
    error). These are the pretraining convergence/overfitting diagnostics.
  * Note: F1-score does not apply to pretraining (no labels); the pretraining
    objective is reconstruction loss.

5.6 Hyperparameter policy: FIXED, DISCLOSED, CONSTANT ACROSS RUNS

  Definition. "SSL knobs" are the hyperparameters that exist only because of
  the self-supervised pretraining stage and have no counterpart in the baseline
  supervised M3 (and are therefore not covered by M3's Optuna search):
    - masking ratio (fraction of the window masked);
    - mask span length (length of each contiguous masked segment);
    - decoder architecture (depth/width of the discarded reconstruction head);
    - reconstruction loss form (MSE vs L1; time-only vs +spectral term);
    - SSL optimiser schedule (pretraining LR, warmup, batch size, epochs);
    - fraction of D_full consumed (subsampling for the ~33M-segment job).

  Policy. This study does NOT tune the SSL knobs to maximise performance.
    (i)   The encoder architecture and the supervised fine-tuning recipe are
          inherited UNCHANGED from the selected M3 configuration, so encoder
          capacity is identical across R0-R4.
    (ii)  The SSL knobs are fixed to literature-informed defaults for masked
          autoencoding, DISCLOSED in this document, and held IDENTICAL across
          R1-R4. They are not searched on downstream validation F1.
    (iii) Because the SSL recipe is held constant, the only quantities that
          vary between R1, R2 and R3 are the ENCODER INITIALISATION and the
          DATA distribution -- preserving the one-variable-at-a-time logic of
          the run matrix (Section 7; causal comparison map in
          RESULTS_REPORTING_PLAN.txt Section 1.2).

  Rationale. This is a controlled causal study of data-distribution effects,
  not a leaderboard optimisation. Two practices are therefore explicitly
  avoided because they would confound or weaken the causal claim:
    - Per-run tuning (different knobs for R1 vs R2 vs R3) would confound
      "data exposure" with "different recipe", destroying the R1->R2->R3
      contrast.
    - A large downstream-validation search would invite the criticism that
      gains stem from tuning rather than from SSL/data, and -- since R0/R4 use
      fixed recipes -- would introduce an asymmetric advantage.
  Fixing and disclosing the knobs is both the most defensible option and the
  scientifically stronger one, because the controlled comparison is most
  convincing when only the studied variable moves.

  Permitted exception (optional). A SINGLE, transparent masking-ratio sweep
  {25% / 50% / 75%} may be reported as an ABLATION (characterising sensitivity,
  not selecting a winner). If run, the chosen ratio is decided once and then
  held constant across all runs; the sweep is reported in full. This is the
  only place any SSL knob is varied, and it is framed as robustness
  characterisation, not tuning.

--------------------------------------------------------------------------------
6. DOWNSTREAM ADAPTATION PROTOCOLS
--------------------------------------------------------------------------------

After pretraining, the SSL decoder is discarded and the linear classification
head is attached. Each SSL run is adapted under two protocols:

  P1  Frozen (linear probe). Encoder weights are frozen; only the linear head
      is trained. Isolates the quality of the learned REPRESENTATION.

  P2  Full fine-tune. Encoder and head are trained end-to-end. Matches the
      baseline deployment regime and measures the initialisation benefit.

The supervised fine-tuning recipe (loss BCEWithLogitsLoss, optimiser, LR
schedule, early stopping, post-processing) is inherited unchanged from the
baseline M3 training, so that the only differences from R0 are the encoder
initialisation and, in R3/R4, the fine-tuning data distribution.

6.1 Data usage and no-leakage policy (fine-tuning)

  * Fine-tuning gradients come ONLY from training data (D0 or D_broad, built
    from training-partition subjects).
  * The validation partition is used ONLY for early stopping and model
    selection -- never for gradient updates.
  * NO thresholding is performed. The decision rule is fixed at P > 0.5, as in
    the baseline project; the threshold is not tuned on validation.
  * The decision THRESHOLD is not tuned (fixed P > 0.5). The min-event
    OPERATING POINT is handled EXACTLY as in DL_WITH_SSL_GA: reuse/adapt
    postproc_sweep.py to sweep {10,15,20,25,30 s} offline on cached predictions
    (order fixed to the baseline's min_then_refractory), and select the
    operating point on validation from the Recall(sensitivity)-vs-FAR/hr Pareto
    / impact plots -- the point in the BOTTOM-RIGHT ideal region (high
    sensitivity, low FAR/hr) -- just as the baseline selected its 25 s. NO fixed
    FAR/hr ceiling. The selected operating point is then applied on test.
  * For comparability with the baseline, results are ALSO reported at the common
    25 s point, and the full min-event sweep is reported as a curve
    (RESULTS_REPORTING_PLAN.txt Section 2.1). The test partition is evaluated
    once, at the end.

6.2 Training diagnostics reported (fine-tuning; every trained run)

  * Train and validation LOSS (BCEWithLogitsLoss) curves per epoch.
  * Validation MACRO-F1 per epoch -- the early-stopping / model-selection
    metric (as in the baseline). F1 is a selection/monitoring metric, NOT the
    differentiable training objective (which is BCE); this distinction is kept
    explicit in the write-up.

--------------------------------------------------------------------------------
7. RUN MATRIX (5-RUN COMPARISON MATRIX; 4 RUNS DEVELOPED HERE)
--------------------------------------------------------------------------------

Scope note. The comparison matrix spans FIVE runs (R0-R4) because R0 is the
REFERENCE ANCHOR every contrast is measured against. Only FOUR runs -- R1, R2,
R3, R4 -- are DEVELOPED (trained) in this project. R0 is reused from the parent
project (its metrics are cited; its encoder is loaded only for the embedding
comparison). Protocols: R1-R3 run frozen + full; R0/R4 are full-only.

  Run  Encoder init  SSL data   Fine-tune data   Frozen(P1)   Full-FT(P2)
  ---  ------------  --------   --------------   ----------   -----------
  R0   random        --         D0               n/a          existing*
  R1   SSL           D0         D0               yes          yes
  R2   SSL           D_full     D0               yes          yes
  R3   SSL           D_full     D_broad          yes          yes
  R4   random        --         D_broad          n/a          yes

  * R0 is NOT developed in this project. It is the existing supervised M3 from
    DL_WITH_SSL_GA. Its published metrics are cited as the reference point, and
    its trained encoder is loaded only for the embedding comparison
    (RESULTS_REPORTING_PLAN.txt Section 2.3). Frozen probing is not applicable
    to R0/R4 as they have no SSL encoder to freeze.

Only R1-R4 are trained in the present study.

--------------------------------------------------------------------------------
8. EVALUATION FRAMEWORK (THREE LAYERS)  -- MOVED
--------------------------------------------------------------------------------

The three-layer evaluation framework (what is measured) has been MOVED to the
companion document:

    RESULTS_REPORTING_PLAN.txt, Section 2 -- EVALUATION FRAMEWORK (THREE LAYERS)

It defines, unchanged:
    Layer 1  Standard performance metrics (eval_utils.py; segment + event
             level, fixed four-stage post-processing, min-event sweep).
    Layer 2  Artefact-proxy stratified false-alarm number (stratum A vs
             control B; multi-feature seizure-mimicking-non-ictal proxy).
    Layer 3  Representation (embedding) analysis (UMAP/t-SNE + probe AUROC +
             silhouette across all encoders R0-R4).

Rationale for the move: all evaluation-and-reporting content now lives in one
place (RESULTS_REPORTING_PLAN.txt), which also holds the hypothesis
traceability matrix, main-text table/figure plan, statistical tests, and the
min-event sweep mechanics. This section number is retained as a pointer so that
Sections 9-13 and all existing cross-references remain valid.

--------------------------------------------------------------------------------
9. CAUSAL COMPARISON MAP (WHAT EACH CONTRAST PROVES)  -- MOVED
--------------------------------------------------------------------------------

The causal comparison map (what each contrast proves, and the primary evidence
layer for each) has been MOVED to the companion document, alongside the
hypothesis traceability matrix it complements:

    RESULTS_REPORTING_PLAN.txt, Section 1.2 -- Causal comparison map

Rationale for the move: the causal-contrast narrative belongs with the results-
organising material (hypotheses, traceability matrix, tables/figures, tests),
so the reader finds "what each contrast proves" and "how it is reported" in one
place. The hypotheses themselves remain in Section 2 of this document. This
section number is retained as a pointer so that Sections 10-13 and all existing
cross-references remain valid.

--------------------------------------------------------------------------------
10. METHODOLOGICAL FLOW (SCHEMATIC)
--------------------------------------------------------------------------------

  STAGE 0  ASSETS (reused from DL_WITH_SSL_GA)
           Encoder MultiScaleTCN(M3) | R0 existing | D0, D_full, D_broad
                        |
  STAGE 1  SSL PRETRAINING  (Masked EEG Autoencoder; encoder only, no labels)
           R1 <- D0 ;  R2,R3 <- D_full
                        |  pretrained encoder weights
  STAGE 2  DOWNSTREAM ADAPTATION
           P1 Frozen linear probe  |  P2 Full fine-tune
           fine-tune data: D0 (R1,R2) or D_broad (R3,R4); recipe = R0's
                        |
  STAGE 3  RUN MATRIX  (5 runs x 2 protocols; Section 7)
                        |
  STAGE 4  EVALUATION  (RESULTS_REPORTING_PLAN.txt Section 2)
           L1 standard metrics (eval_utils.py)
           L2 artefact-proxy stratified FA  (stratum A vs control B)
           L3 embedding analysis (UMAP/t-SNE + probe AUROC + silhouette)
                        |
  STAGE 5  CAUSAL COMPARISONS  (RESULTS_REPORTING_PLAN.txt Section 1.2)  ->  H1-H6

--------------------------------------------------------------------------------
11. REPRODUCIBILITY AND DOCUMENTATION CONVENTIONS
--------------------------------------------------------------------------------

  * Deterministic seeding (seed 42) across Python, NumPy, and PyTorch, matching
    the baseline.
  * The baseline encoder, data pipeline, and eval_utils.py are imported /
    reused unchanged; the exact commit/version reference is recorded here when
    the code is written.
  * Every fixed value currently marked [TBD] (masking ratio, D_broad
    construction, proxy features and percentiles, optimiser/schedule/epochs) is
    fixed on the validation partition and recorded in this document before any
    test-set number is produced.
  * Each implementation step, decision, and result is appended to this file as
    work proceeds, in the scientific register of the baseline STUDY_REPORT.

--------------------------------------------------------------------------------
12. OPEN PARAMETERS TO BE FIXED DURING IMPLEMENTATION
--------------------------------------------------------------------------------

  Note: the SSL knobs below ([TBD-1..3]) are FIXED to disclosed defaults and
  held constant across R1-R4 per the policy in Section 5.6 -- they are NOT
  tuned on downstream validation. "[TBD]" here means "value to be recorded once
  chosen", not "to be searched".

  [TBD-1]  Masking ratio and span length for the masked autoencoder (fixed;
           optional single ablation sweep {25/50/75%} per Section 5.6).
  [TBD-2]  Decoder architecture and reconstruction loss form; whether a
           spectral (STFT) reconstruction term is added (fixed, disclosed).
  [TBD-3]  SSL optimiser, LR schedule, batch size, epochs, pretraining data
           subsampling for the ~33M-segment D_full job (fixed, disclosed).
  [TBD-4]  RESOLVED (2026-07-25): D_broad ratio R=6 (~393k non-ictal). Disclosed
           design choice, NOT tuned on validation; pos_weight stays 1.0.
  [TBD-5]  Artefact-proxy feature set, standardisation, composite score, and
           selection percentiles; seizure-distance margin (60 s vs 120 s).
  [TBD-6]  Embedding-analysis sample sizes per group.
  [TBD-7]  Min-event operating point -- selected on the validation
           Recall-vs-FAR/hr Pareto / impact plots (bottom-right ideal region) as
           in DL_WITH_SSL_GA via postproc_sweep.py. An optional deterministic
           rule (min normalised distance to the bottom-right corner) may be used
           for full reproducibility. NO FAR/hr ceiling. Recorded once chosen.
  [TBD-8]  RESOLVED (audit 2026-07-25): counts + D_full enumeration recorded in
           Section 3.1. SSL loader reads the D_full manifest directly (no scan).

--------------------------------------------------------------------------------
13. IMPLEMENTATION / BUILD PLAN (METHOD SCRIPTS)
--------------------------------------------------------------------------------

Order in which the METHOD code is written. Evaluation-stage scripts are recorded
separately in RESULTS_REPORTING_PLAN.txt Section 10. Scripts are built in
dependency order, and a VERTICAL SLICE on R1 alone (SSL pretrain -> fine-tune ->
cache predictions -> one metric) is completed and verified BEFORE scaling to
R2-R4 and adding evaluation Layers 2/3, so integration bugs surface on one cheap
run rather than after four expensive ones.

Global build decisions (shared with the evaluation plan).
  * VENDORING: the baseline encoder (tcn_utils.MultiScaleTCN) and evaluation
    utilities (eval_utils.py) are VENDORED as a PINNED COPY into src/vendor/
    (source commit hash recorded), NOT imported live -- the repo is
    self-contained and the encoder cannot drift from R0.
  * ORCHESTRATION: every stage has a Slurm .sh wrapper matching the baseline
    DL_WITH_SSL_GA .sh convention.
  * Deterministic seeding (seed 42), as in the baseline.

Directory layout (repo):
  src/       core importable modules
  scripts/   runnable drivers + Slurm .sh wrappers (one per stage)
  configs/   per-run configs (R1..R4) + global paths / constants

Cluster paths and OUTPUT layout.
  * INPUTS (derived): /home/people/22206468/scratch/INPUT_SSL_DATA_DISTRIBUTION/
    -- the D_broad manifest, artefact-proxy stratum lists, and any derived input
    are WRITTEN here and read back (as the parent used INPUT_DATA/
    data_splits_outputs/). Reused parent manifests (D0, D_full, val/test) are
    read from the parent's data_splits_outputs/.
  * OUTPUTS: /home/people/22206468/scratch/OUTPUT_SSL_DATA_DISTRIBUTION/
      R1/  SSL_PRETRAINING/  FROZEN_FINE_TUNING/  FULL_FINE_TUNING/
      R2/  SSL_PRETRAINING/  FROZEN_FINE_TUNING/  FULL_FINE_TUNING/
      R3/  SSL_PRETRAINING/  FROZEN_FINE_TUNING/  FULL_FINE_TUNING/
      R4/                                          FULL_FINE_TUNING/  (random
                                                   init; full-only)
      ANALYSIS/  -- cross-run outputs (embedding analysis, tables, figures,
                    benchmark) that are not per-run.
    Under each fine-tuning folder the post-processing sweep writes its baseline
    output tree (RESULTS_REPORTING_PLAN.txt Section 6.1).

  * SHARED D_full PRETRAINING: R2 and R3 pretrain on D_full with the IDENTICAL
    recipe, so the encoder is trained ONCE, not twice (the most expensive step;
    ~28.7M segments). The shared encoder is the canonical artefact reused by
    both. R2 and R3 EACH still keep their own logs / outputs in their respective
    SSL_PRETRAINING folders, recording provenance of the shared encoder. R1
    pretrains separately on D0. => TWO SSL pretrainings total: D0 (for R1) and
    D_full (for R2 + R3).

  * SINGLE SEED (42) throughout -- one seed level only, no per-seed folders.
    Reproducible and matched to the baseline; statistical variance comes from
    the subject population, not repeated seeds (RESULTS_REPORTING_PLAN.txt
    Section 5 / 9.6).

  * LOGGING: every script writes a log; specifics in the Infrastructure
    conventions block below.

Infrastructure conventions (MIRROR DL_WITH_SSL_GA; adapt only where noted).

  SEEDING -- src/common/seed.py set_seed(42): seeds python random, numpy, torch
    CPU (manual_seed), torch CUDA all devices (manual_seed_all); sets
    cudnn.deterministic=True, cudnn.benchmark=False. Call at main() entry AND
    immediately before model weight init (identical start every run).

  CHECKPOINTING -- per stage (SSL pretrain / frozen / full), under
    <stage>/checkpoints/. save_checkpoint() payload: epoch, model_state,
    optimiser_state, scheduler_state, monitor_metric, train_loss, best_metric,
    best_epoch, epochs_no_imp (early-stop counter), hyperparameters, timestamp,
    and embedded best_model_state (CPU-cloned best weights). Wrapped in
    try/except (non-fatal). Three files:
      {prefix}_latest.pt  -- written EVERY epoch (clean resume);
      {prefix}_best.pt    -- written on improvement;
      {prefix}_epoch_*.pt -- periodic every CHECKPOINT_FREQ=5, pruned to
                             KEEP_CKPTS=3 (best/latest never pruned).
    ADAPTATION -- the "best" / early-stopping metric differs by stage:
      * SSL pretraining  -> LOWEST validation RECONSTRUCTION loss;
      * frozen / full FT -> HIGHEST validation MACRO-F1 (as baseline).
    RNG state is NOT saved (resume not bit-identical; acceptable, per baseline).

  RESUME -- at main() start: prefer {prefix}_latest.pt, fallback
    {prefix}_best.pt, else fresh; restore model/optimiser/scheduler/best/
    epochs_no_imp; start_epoch = ckpt["epoch"]+1; loop range(start_epoch,
    MAX_EPOCHS+1). Re-submitting the same Slurm job AUTO-RESUMES; delete
    checkpoints/ to restart.

  LOGGING -- setup_logging(): named logger; DEBUG FileHandler (append mode "a")
    to <stage>/logs/<script>.log + INFO StreamHandler(stdout); format
    "%(asctime)s | %(levelname)s | %(message)s"; handlers.clear() first (no
    duplicate handlers on resume). One log file per script (appended across
    resumes; Slurm %j files are the per-run copies). Per-epoch line: epoch,
    loss, monitor metric, LR, best, patience, train/val timing. Start/end
    banners + an output-inventory (OK/MISSING) check at the end.

  OUTPUT (per run x stage): <stage>/{checkpoints/, logs/, figures/} + final
    weights (CPU state_dict .pt) + json/csv artifacts + predictions .npz
    (enriched with chronology). Nested under OUTPUT_SSL_DATA_DISTRIBUTION/R{n}/
    {SSL_PRETRAINING|FROZEN_FINE_TUNING|FULL_FINE_TUNING}/.

  SLURM .sh (one per stage): #!/bin/bash -l; SBATCH --job-name, -N 1,
    --ntasks=1, --cpus-per-task=10, --partition=csgpu, --gres=gpu:1, -t <long>,
    --mail-type=ALL, --mail-user=mercy.edoho@ucdconnect.ie, --output=...-%j.out;
    diagnostics echoes (date, hostname, JOB ID, nvidia-smi, CPU alloc);
    module purge && module load anaconda3 && conda activate torch_v100_py310;
    cd <repo>; python <driver>.py. (Reuse the baseline conda env
    torch_v100_py310.)

Phase 0 -- Scaffolding (written first; unblocks everything)
  1. src/common/config.py, seed.py -- paths, constants, deterministic seeding.
  2. src/vendor/ -- pinned copy of tcn_utils.py (MultiScaleTCN) + eval_utils.py
     (commit hash recorded).
  3. scripts/audit_manifests.py -- read manifest metadata; verify D_full
     enumeration; record train/val/test counts. Resolves [TBD-8].

Phase 1 -- Data layer
  4. src/data/datasets.py -- SSLWindowDataset (masked, unlabeled) + reuse of the
     vendored EEGSegmentDataset for supervised loading.
  5. scripts/build_broad_manifest.py -- construct D_broad from data_splits.json
     train (adapts create_balanced_splits.py). Resolves [TBD-4].

Phase 2 -- SSL pretraining (the new core)
  6. src/models/masked_autoencoder.py -- M3 encoder + span-masking + decoder +
     reconstruction loss. Resolves [TBD-1, TBD-2].
  7. scripts/pretrain_ssl.py (+ .sh) -- SSL training loop; validation
     reconstruction monitor; checkpoint-resume. Resolves [TBD-3]. Runs: R1 on
     D0; R2 & R3 on D_full (train subjects only; Section 5.4).

Phase 3 -- Downstream adaptation
  8. scripts/finetune.py (+ .sh) -- load pretrained encoder; attach linear head;
     P1 frozen (linear probe) / P2 full fine-tune; recipe vendored from the M3
     baseline (AdamW, cosine LR, BCEWithLogitsLoss, early stop on val macro-F1).
  9. configs/R1..R4.yaml -- per-run wiring (encoder init, SSL data, fine-tune
     data, protocol). R1-R4 developed here; R0 reused (Section 7).

Evaluation-stage scripts (prediction caching, min-event sweep, artefact-proxy,
embedding analysis, statistics, tables/figures) continue in
RESULTS_REPORTING_PLAN.txt Section 10.

--------------------------------------------------------------------------------
14. REFERENCES (TO BE COMPLETED AT WRITE-UP)
--------------------------------------------------------------------------------

  Baseline project methods    : DL_WITH_SSL_GA/STUDY_REPORT.txt
  Masked autoencoding (SSL)   : [to be cited]
  Contrastive TS SSL (context): [to be cited]
  EEG artefact morphology     : [to be cited]

================================================================================
 END OF DOCUMENT (living; updated as implementation proceeds)
================================================================================
